{
  "name": "wokitokie",
  "private": true,
  "workspaces": ["packages/shared", "packages/server", "packages/phone", "packages/dock"],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build"
  },
  "packageManager": "npm@10.9.2",
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.4.0"
  }
}
